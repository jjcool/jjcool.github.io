[[output-set]]
=== Output Set
Output set section provides functionality to generate output data record for mutating or non-mutating(Information Request) data source.

*Data Source*: To specify input data source and output data record name.

[source,gherkin]
----
include::code/sample.feature[tag=outputds]
----

*Output Format*: Format in which the ARAF will generate the output. Note that Supported formats are CSV, JSON

[source,gherkin]
----
include::code/sample.feature[tag=outputformat]
----
Detailed description <<output_records,here>>

*Output Variables*: Define the variables which should be dumped in the output record.
There are 2 filters which can be used to filter the variables -

*select* - If user want only specific variables in the output record.
[source,gherkin]
----
include::code/sample.feature[tag=outputvars]
----

*all* - If user want all the generated variables in the output record.
[source,gherkin]
----
Then output all values
----
